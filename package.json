{
  "name": "amc-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node build-inject.js && vite build",
    "preview": "vite preview",
    "inject": "node build-inject.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}